apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: open-webui-pro-api-cert
  namespace: istio-ingress
spec:
  dnsNames:
  - ai.condoconta.info
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: route53-issuer
  secretName: open-webbui-pro-api-cert

